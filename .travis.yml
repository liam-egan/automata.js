language: node_js
node_js:
  - node

script:
  - npm run test
  - npm run build
after_success:
  - npm run docs

deploy:
  - provider: pages
    local-dir: docs/
    skip-cleanup: true
    github-token: $GH_TOKEN
    keep-history: true
    target-branch: gh-pages
    on:
      branch: master
